import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gripe-generator-v1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
